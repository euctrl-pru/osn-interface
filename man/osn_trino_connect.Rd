% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trino.R
\name{osn_trino_connect}
\alias{osn_trino_connect}
\title{Open a connection to the OpenSky Network Trino endpoint}
\usage{
osn_trino_connect(force_login = FALSE)
}
\arguments{
\item{force_login}{Logical. If \code{TRUE}, ignore any cached token and force a
fresh browser login.}
}
\value{
A \code{PrestoConnection} (DBI connection) to the OSN Trino server.
}
\description{
Connects to \code{trino.opensky-network.org} (catalog \code{minio}, schema \code{osky})
using the native R Trino client \code{RPresto}. OpenSky's Trino endpoint uses
OAuth2 external authentication (the same browser-based flow as
\code{trino --external-authentication}): on first use a browser window opens for
you to log in with your OpenSky account; the resulting bearer token is cached
on disk and reused until it expires. This is the backend used by the
\code{"osn-historical-trino"} source (see \code{\link[=osn_connect]{osn_connect()}}).
}
\details{
The \code{OPENSKY_USERNAME} environment variable is used as the Trino user tag
(\code{X-Trino-User}); the actual authentication happens via the browser login,
not \code{OPENSKY_PASSWORD}.

Requires the \code{RPresto}, \code{httr} and \code{jsonlite} packages (in Suggests); install
them with \code{install.packages(c("RPresto", "httr", "jsonlite"))} if missing.
}
